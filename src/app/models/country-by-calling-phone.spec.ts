import { CountryByCallingPhone } from './country-by-calling-phone';

describe('CountryByCallingPhone', () => {
  it('should create an instance', () => {
    expect(new CountryByCallingPhone()).toBeTruthy();
  });
});
